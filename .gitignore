# —— 不发布到公开仓库的本地文件 ——
# 会话记录导出（含聊天原文，只留本地）
(压缩1)*.md
# 临时测试 / 中间产物
_tmp-*
tmp/
*.bak
# 整本生成的进度文件（跑完就删；别把过程数据发上去）
data/.gen-*.json
# 其它工具的本地目录（WorkBuddy / Claude / Codex 等）
.workbuddy/
.claude/
.codex/
.hermes/
